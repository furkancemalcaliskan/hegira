schema = 2
id = "layered-base"
version = "v0.5.0"
source = "applications/layered"
include = [
  ".dockerignore",
  ".env.example",
  ".gitignore",
  "Cargo.lock",
  "config",
  "crates",
  "docker-compose.yml",
  "hegira.toml",
]
optional_dependencies = ["layered-leptos-identity"]
