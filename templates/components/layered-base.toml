schema = 3
id = "layered-base"
version = "v0.6.0"
source = "applications/layered"
include = [
  ".node-version",
  ".dockerignore",
  ".gitignore",
  "crates/application",
  "crates/application_contracts",
  "crates/domain",
  "crates/domain_shared",
  "hegira.toml",
  "rust-toolchain.toml",
  "scripts/prepare-wasm-bindgen.sh",
]
optional_dependencies = ["identity", "layered-leptos-identity", "layered-leptos-minimal"]
