# Copy this file to .env, choose a local-only password, and source it before
# running the application. Docker Compose reads the same file automatically.
POSTGRES_PASSWORD=
APP__DATABASE__URL=postgres://postgres:${POSTGRES_PASSWORD}@localhost:5432/application
APP__SECURITY__JWT_SECRET=
APP__SEED__ADMIN_PASSWORD=
