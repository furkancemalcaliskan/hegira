[package]
name = "application_mutator"
description = "Deterministic existing-application change planning for the Hegira full-stack application framework"
version = "0.4.0"
edition = "2024"
license = "Apache-2.0"
publish = false
rust-version = "1.96"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
sha2 = "0.10.9"

[dev-dependencies]
serde_json = "1.0.149"
