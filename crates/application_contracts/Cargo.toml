[package]
name = "application_contracts"
version = "0.3.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
identity_application_contracts = { path = "../../modules/identity/application_contracts" }

[features]
openapi = ["identity_application_contracts/openapi"]
